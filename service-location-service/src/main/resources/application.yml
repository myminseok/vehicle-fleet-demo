server:
  port: 9001

spring:
  application:
    name: service-location-service
  data:
    mongodb:
      uri: ${vcap.services.${PREFIX:}mongodb.credentials.uri}

security:
  basic:
    enabled: false
